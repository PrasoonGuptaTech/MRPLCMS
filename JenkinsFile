pipeline {
    agent any

    tools {
        nodejs 'Node23'
    }

    environment {
        ANDROID_HOME = "/Users/prasoongupta/Library/Android/sdk"
        ANDROID_SDK_ROOT = "/Users/prasoongupta/Library/Android/sdk"
        PATH = "${ANDROID_HOME}/platform-tools:${ANDROID_HOME}/cmdline-tools/latest/bin:${env.PATH}"
    }

    stages {

        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install') {
            steps {
                sh 'node -v'
                sh 'npm -v'
                sh 'npm install'
            }
        }

        stage('Test') {
            steps {
                sh 'npm test -- --watchAll=false'
            }
        }

        stage('Check Android SDK') {
            steps {
                sh '''
                    echo "ANDROID_HOME=$ANDROID_HOME"
                    echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
                    ls -la $ANDROID_HOME
                '''
            }
        }

        stage('Build Android') {
            steps {
                dir('android') {
                    sh './gradlew assembleDebug'
                }
            }
        }
    }
}